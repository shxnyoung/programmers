def solution(arr):
    answer = []
    minnum = arr[0]
    if len(arr) == 1:
        return [-1]
        
    for i in arr:
        if i < minnum:
            minnum = i
    arr.remove(minnum)
    answer = arr
    return answer
